# What is lambda func􀆟on in python? What we call a func􀆟on which is
# incomplete version of a func􀆟on?

#lamda is a anonoymous function which has no name . one or more arguments but only one expression.
# syntax like this : lambda arguments:expression
# returns an object
#  
square=lambda num:num*num
print(f"Using lambda square of 5:{square(5)}")